import { modelsPostsModels } from './models-posts-models';

describe('modelsPostsModels', () => {
  it('should work', () => {
    expect(modelsPostsModels()).toEqual('models-posts-models');
  });
});
